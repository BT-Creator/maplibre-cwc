// TODO: Fix render test

/* import { newE2EPage } from '@stencil/core/testing';

describe('maplibre-popup', () => {
  it('renders', async () => {
    const page = await newE2EPage();
    await page.setContent('<maplibre-map><maplibre-popup lng-lat="[0,3]" id="test-element"><p>Test</p></maplibre-popup></maplibre-map>');

    const element = await page.find('maplibre-map >>>> #test-element');
    expect(element).toHaveClass('hydrated');
    expect(element).toHaveAttribute('lng-lat');
  });
});
*/
